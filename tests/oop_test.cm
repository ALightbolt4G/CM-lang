struct User {
    name: string;
    age: int;
}

impl User {
    fn greet(self: User) {
        println("Hello from User!");
    }

    fn have_birthday(self: User) {
        println("Happy Birthday!");
    }
}

fn main() {
    let u: User;
    u.greet();
    u.have_birthday();
}
